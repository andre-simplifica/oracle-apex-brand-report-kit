interface:
  display_name: "Build APEX Brand Reports"
  short_description: "Build branded Oracle APEX reports safely"
  default_prompt: "Use $build-apex-brand-reports to create a branded Oracle APEX report with print, PDF, and server-side exports through the agent-native workflow; treat Python scripts as optional tooling."
